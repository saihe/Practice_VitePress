{
  "scripts": {
    "dev": "npx vitepress dev docs",
    "build": "npx vitepress build docs",
    "preview": "npx vitepress preview docs",
    "deploy": "npx swa deploy --print-token"
  },
  "devDependencies": {
    "@azure/static-web-apps-cli": "^1.1.7",
    "vitepress": "^1.1.0",
    "vitest": "^1.5.0"
  }
}
